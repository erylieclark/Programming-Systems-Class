#include <stdio.h>



int main(void){
    char *cp;
    *cp = 'c';
    printf("cp points to %c", *cp);
    char (*ap)[1,2,3,4,5];
    printf("ap points to %c", *ap);
    int pp**;
    printf("value of pp is %d\n", pp);
    return 0;
}
